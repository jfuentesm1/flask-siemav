

'''@app.route('/')
def index():

    ip_cliente = request.remote_addr

    lista_usuarios = {
        'jorge': '192.168.100.77',
        'laptop': '192.168.100.65',
    }
    for usuario, ip in lista_usuarios.items():
        if ip == ip_cliente:
            return f'bienvenido a siemav {usuario}'

    return f'No existe la dirección IP en el diccionario, {ip_cliente}'''